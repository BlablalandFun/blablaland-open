import maps from '../../files/maps.json' assert { type: 'json' };
import servers from '../../files/servers.json' assert { type: 'json' };
import objects from '../../files/objects.json' assert { type: 'json' };


export {
  maps as DBMaps,
  servers as DBServers,
  objects as DBObjects,
}